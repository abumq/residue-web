const React = require('react');

// require './style.css';

const Home = React.createClass({
  render() {
    return (
      <div className="page Home">
        <p className="Home__intro">
          Welcome to residue web UI
        </p>
      </div>
    );
  }
});

export default Home;
